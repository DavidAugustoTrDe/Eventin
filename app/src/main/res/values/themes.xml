<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Eventin" parent="Theme.MaterialComponents.DayNight.NoActionBar"> <!-- Hiding the ActionBar with the parent -->
        <item name="colorPrimary">@color/logo_colorblack</item>
        <item name="colorPrimaryVariant">@color/logo_colorblack</item>
        <item name="colorOnPrimary">@color/logo_colorblack</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">@color/gray_pewter</item>
    </style>

    <style name="splashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splashscreen</item>
        <item name="android:statusBarColor">@color/logo_colorblack</item>
    </style>

    <style name="chipGEntry" parent="Widget.MaterialComponents.Chip.Entry">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
        <item name="chipBackgroundColor">@color/white</item>
        <item name="chipStrokeColor">@color/blue_baby</item>
        <item name="chipStrokeWidth">2dp</item>
    </style>

</resources>